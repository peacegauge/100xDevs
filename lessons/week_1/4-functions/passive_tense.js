//Write a function to calculate the sum and return the result in the passive voice

let passive_sum = function(a, b){
    return `The sum was calculated to be ${a + b}`;
};

console.log(passive_sum(4, 45))
